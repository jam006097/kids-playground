# Django settings
# IMPORTANT: Keep this key secret in production!
# You can generate a new key using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY='django-insecure-dummy-key-for-development'

# Set to False in production
DEBUG=True

# Database settings for Docker Compose
POSTGRES_DB=kids_db
DB_USER=kids_user
DB_PASSWORD=kids_password

# AI API URL (referenced by docker-compose.yml)
# This should match the port mapping for the ai-api service
AI_API_URL=http://localhost:8001
